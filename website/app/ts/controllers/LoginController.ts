"use strict";

/**
 * Represents the interface for the feature controller's scope
 */
interface ILoginControllerScope extends ng.IScope {
}

/**
 * Login controller
 */
docsApp.controller("featuresController", function($scope: ILoginControllerScope) {

});