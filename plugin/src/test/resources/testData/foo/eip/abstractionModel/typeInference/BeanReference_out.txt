Route(
     From(direct:start,TODO)
     Bean(Some(orderFactory),Some(createOrder),TODO)
     To(direct:bar,TODO)
     Bean(Some(complexFactory),Some(createComplexModel),TODO)
     Bean(Some(),None,TODO)
     To(direct:foo,TODO)
)