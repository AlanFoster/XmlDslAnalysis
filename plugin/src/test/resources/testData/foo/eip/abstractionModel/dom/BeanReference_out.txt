Route(
     From(direct:start)
     Bean(Some(orderFactory),Some(createOrder))
     To(direct:bar)
     Bean(Some(complexFactory),Some(createComplexModel))
     Bean(Some(),None)
     To(direct:foo)
)